using System;

class Principal{
    static void Main(){
        float A = float.Parse(Console.ReadLine());
        float B = float.Parse(Console.ReadLine());
        
        Console.WriteLine("Media = {0}", (A * 3.5) + (B * 7.5));
    
    }
}