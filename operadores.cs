using System;

class alua5{

    static void Main(){

        //res++   //res += 1;  //res = res + 1;
        //bool res = 10<5;

        // & = and / e
        // | = or / ou

        bool res = (5<7) | (10<5);

        bool ress = (8>9) & (6<9);

        Console.WriteLine(res+ " or "+" and "+ress);

    }
}